meta {
  name: Upload Image
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/images
}

headers {
  Authorization: Bearer {{adminToken}}
}

body:multipart-form {
  image: @file({{imageFile}})
  remark: {{imageRemark}}
}
